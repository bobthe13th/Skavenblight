# SDD ledger — plan: docs/superpowers/plans/2026-07-31-staircase-siege-group-gametest-plan.md
Task 1: complete (commits 44c023b..3dccbcf, review clean). Generated pathing_test_giant.nbt (349034 bytes, 64x64x32), generator+smoke-test scaffolding removed before commit.
Task 2: first attempt BLOCKED (agent a362951bbee59ffe8) - real bug found: WarpFluxNetwork's default territory radius reaches GameTest's own encasement ledge regardless of geometry margin, and FlowFieldCalculator's cycle-breaker sacrifices the real locked BUILD_STAIR instead of the bogus one. Root-caused via 5 runs + 4 reverted diagnostics, no commit made (correctly, per instructions not to claim a pass without verification). Plan revised (commit e55f43d): added restrictTerritoryToMinimalArea helper using WarpFluxNetwork#getTerritoryChunks' live mutable set, applied to all 4 tasks, Task 5's Config.territoryChunkRadius mutation removed entirely. Re-dispatching same agent with regenerated brief.
Task 2: second attempt BLOCKED (same agent) - restrictTerritoryToMinimalArea did not fix it. Precise coordinate analysis of the failure logs shows the leaked/dropped BUILD_STAIR sits exactly at the structure's own edge (relative x=31/32 in a 32-wide/2-chunk template) - NOT a "scanner peeks 1 chunk beyond territory" mechanism (verified directly against FlowFieldState.isOutOfBounds's strict territoryChunks.contains check - no such leniency exists in that code path). Root cause: the 14-block gap already consumes most of a 32-wide (2-chunk) template's width, so ANY minimal territory covering it unavoidably reaches the structure's own edge where encasement sits - margin was never the fixable lever, template-size-relative-to-gap-size is. Revising plan again: move all 4 tests onto the (resized, larger) pathing_test_giant template with generous absolute margins (>16 blocks/1 chunk from every edge, verified against exact new coordinates), eliminating pathing_test_tall from this plan entirely. Redoing Task 1 (resize 64x64x32 -> 96x96x32) before re-dispatching Task 2 again.
Plan revised again (commit 443cc96): all 4 tests moved to a resized pathing_test_giant (96x64x96), coordinates updated with >=16-block margin from every edge, pathing_test_tall dropped from this plan entirely. Redispatching Task 1 (agent a821da4f748e6cf81) to resize the template before retrying Task 2 a third time.
Task 1 redo: complete (commits 443cc96..86443cf, review clean). pathing_test_giant.nbt resized to 96x64x96 (1,624,028 bytes). Proceeding to Task 2's third attempt.
Task 2: third attempt BLOCKED (same agent, resized template) - the encasement hazard from attempts 1-2 is CONFIRMED FIXED (does not recur in 4 runs). New, distinct symptom found: rat walks to within ~1 block of the nexus horizontally then sits motionless at ground level; confirmed via temporary reverted diagnostic logging that region resolution + RegionRouteTree.isReachable both succeed correctly (not a stranding/generation-mismatch issue) - but zero SiegeProjectManager/BuildFlowFieldGoal/WidenStairsGoal/BUILD_STAIR activity is ever logged, meaning the connector's staircase instruction never reaches the mob's real navigation. Escalating: 3 resumes of the same agent/model tier have each found a genuinely different root cause (real progress, not a stuck loop), but this next layer requires deep tracing across FollowFlowFieldGoal/RegionFlowField/RegionGraph's connector instruction pairing - dispatching a FRESH implementer on a more capable model (opus) per the fix-loop's rounds-4-5 escalation guidance, with a specific evidence-gathering hypothesis from the coordinator's own review of FollowFlowFieldGoal.java.

Task 2: fourth attempt (fresh Opus implementer) hit a session-wide usage limit mid-investigation; coordinator continued the same investigation directly (no subagent) rather than wait ~4h or re-dispatch into the same limit. Found and fixed THREE real, independently-verified production bugs, committed separately from this task's own test file at commit de4a56a (test file itself intentionally left uncommitted - it still does not pass):
  1. TerrainEvaluator.isActionCompleted's MINE case checked only the foot cell; determineMacroAction can pick MINE for a head/ceiling obstruction too, so a mine step could be marked "already done" while the real obstruction sat untouched 1-2 blocks above the foot.
  2. RegionFlowField.getNextSiegeNode's "already built, walk through" substitution used node.pos().above() for every non-MINE action; that's only correct for the pure-vertical climbs (PILLAR/SPIRAL/LADDER). BUILD_STAIR lands the mob AT node.pos() itself, so the old code stranded a mob one block above its own first built stair - a position with no instruction of its own, killing the chain after exactly one step. Confirmed via a manual 40-hop chain-walk that the full staircase sequence now resolves end-to-end.
  3. FollowFlowFieldGoal's plain Navigation.moveTo() call can never climb the FIRST stair of any gap-spanning staircase: that step is approached from a ledge with open air (no floor) at the mob's own Y in that direction (the gap itself), so vanilla's WalkNodeEvaluator never generates an ascend node there - moveTo silently returns a dead 1-node path with isInProgress()=false forever, no error, no retry (which is also why FollowFlowFieldGoal's own "stuck"/escape-hatch logging never fires - that logic's own precondition is isInProgress()==true, which this case never reaches). Confirmed via direct Navigation.createPath probes (all variants: accuracy 0, accuracy 1, the double-arg overload) consistently returning a 1-node path whose lone node is the mob's own start position, plus ground-truth BlockState checks proving the target cell, its headroom, and the placed stair were all correct. Fixed by driving that one short hop directly via MoveControl.setWantedPosition + JumpControl.jump() instead of routing it through A*.

Task 2 STILL BLOCKED after all three fixes: with the fix in place, a DIFFERENT, NEW symptom appears even before the mob ever reaches the stair - on flat ground, well short of the gap, the mob's blockPosition() oscillates within a 2-3 block radius of its own region's local WALK target for the entire remainder of a 6000-tick test without ever closing the last couple of blocks, even though the flow field's own instruction at the mob's position correctly and consistently points one step closer every tick (verified: the raw@rat target is always exactly the mob's own position minus one in the correct axis, never flip-flopping). This is a genuinely different bug from all three fixed above and from the original encasement/territory issue - each of the last 3-4 fix rounds has uncovered a NEW problem in a DIFFERENT place rather than converging, which is the systematic-debugging skill's own explicit signal to stop and question the approach rather than attempt fix #5 blind. Stopping here rather than continuing further live debugging unsupervised.

Verified no regressions from any of the three fixes: full GameTest suite still shows exactly 26/28 passing both before and after (the same 2 known failures - this task's own test, plus the pre-existing unrelated testParentRegionGetsRealInstructionsForSharedConnectorCells failure).

Recommended next step for whoever picks this up: the flat-ground oscillation is almost certainly worth its OWN fresh root-cause investigation (likely in FollowFlowFieldGoal's pathingUpdateTimer/moveTo cadence, or a lane-occupancy/formation interaction, though this is a single-rat test so lane contention seems unlikely) - probably deserves being spun up as a clean, separate investigation rather than continuing to layer fixes onto this already-deep session. Diagnostic technique that worked well throughout: a temporary diagDump() static method (added directly to the test file, removed before every commit) logging region/connector/instruction-chain state plus, for this last bug, direct Navigation.createPath probes with varying accuracy parameters and ground-truth BlockState checks at the target cell and its flanking cell.

Task 2: fifth root cause found via the human partner LIVE-OBSERVING the test in a rendered client with the mod's own DebugFlowFieldReaderItem overlay (not log analysis) - they spotted that the first stair gets built one cell too far out, floating relative to where the rat stood, with the debug arrow pointing back to the true entry cell. Traced this to AbstractSiegeConstructionGoal.findEffectiveNode's lookahead-jump: it peeks one WALK step ahead and, if real work sits right after that, snaps a construction goal straight to it - but the distance gate reused MAX_TARGET_CLAIM_DISTANCE (2.5, sized for tolerating crowd-shove during an ALREADY-claimed build), which is generous enough to let the snap reach a full two flow-field hops away (~2.24 for one orthogonal + one diagonal hop). BuildFlowFieldGoal was claiming and building the first stair while the rat was still two cells back, before ever walking onto the connector's real entry cell. Per the human partner: this lookahead was only ever meant to let rats walk across surfaces more organically than the flow field's strict 8-direction grid - it was never meant to influence what gets targeted for construction at all. Fixed by adding a dedicated LOOKAHEAD_SNAP_DISTANCE=1.5 constant (single legitimate adjacent-interaction range) used only by this lookahead branch, leaving MAX_TARGET_CLAIM_DISTANCE's other uses (execution-time crowd tolerance) untouched. Verified via the same diagnostic trace: the first stair now builds from a position genuinely adjacent to it (distance 1.41).

Also fixed in the same pass: FollowFlowFieldGoal's escape-hatch ("hasn't moved in ~10 ticks") called Navigation.moveTo directly, hitting the exact same vanilla-can't-ascend-a-gap wall as the original jump-assist fix, just via a second, separate call site - its own escape target can just as easily be a just-built stair one hop back across the same gap. Extracted a shared moveOrHop(from, to) helper (jump+MoveControl when climbing within short range and grounded, plain Navigation.moveTo otherwise) and used it at both call sites.

PARKED for a future circle-back (not yet investigated): with the lookahead fix in place, immediately after the first stair is built, the local flow field's own WALK direction starts flip-flopping between two adjacent cells tick to tick (stand at cell A, told to go to B; stand at B, told to go back to A) - not a movement/navigation bug, the FIELD ITSELF is inconsistent between recomputes. Suspect: the region recalculation triggered right after a successful build (AbstractSiegeConstructionGoal.onChainComplete / BuildFlowFieldGoal's override) produces a different tie-break each time in the immediate vicinity of a connector's entry, once the newly-built cell becomes walkable. Not yet root-caused - needs its own fresh investigation (likely FlowFieldCalculator's Dijkstra tie-breaking, or how RegionScanner/TerritoryRegionMap treats a newly-opened cell on recompute).

REFINED (5 repeated runs after both fixes above): "issue 2" isn't one single symptom - it varies run to run. Some runs oscillate near the gap as described above; others sit completely frozen from tick 0 with zero progress and never even attempt the first ordinary flat WALK step; several end the run mid-air (blockPosition Y like -57.88, -57.5 - between two integer levels) right as the timeout hits. That spread points less at a flow-field consistency bug and more at the JUMP-ASSIST FIX ITSELF (FollowFlowFieldGoal.moveOrHop, commit pending) being imprecise: JumpControl.jump() + MoveControl.setWantedPosition is the generic AI-steering path vanilla uses for opportunistic hops while walking, not a deliberate, precisely-landing leap - across a chain of ~10+ required hops for one staircase, an imprecise landing on any single hop (overshoot, undershoot, barely leaving the ground) plausibly explains every symptom seen: land short and you're back on the same tile (looks frozen); land one tile off and the flow field's own per-cell instruction from there can point somewhere unexpected (looks like flip-flop); get caught mid-arc at timeout (mid-air Y). Replacing moveOrHop's jump+MoveControl with a direct one-tick velocity impulse (mob.setDeltaMovement, sized to actually close the exact horizontal+vertical gap - the same technique vanilla uses for deliberate leaps like Rabbit/Goat jumping) is the next thing to try, in place of the current opportunistic-hop mechanism.

Velocity-impulse replacement implemented and tested (5 more runs): did NOT fix it. All 5 runs now show the rat sitting exactly at Y=-58.00 (grounded, no mid-air Y this time) with zero horizontal delta and Navigation reporting a degenerate Path(length=1) - i.e. it never even reaches an actual climb attempt; a fresh goal-name dump (rat.getActiveGoalNames(), temporarily added to the test file, reverted after) confirmed FollowFlowFieldGoal genuinely is the active goal and the mob is fully at rest (delta=(0,-0.08,0), pure resting gravity, not physics-frozen). One run also showed a THIRD, unrelated failing test (testThreeRegionsRouteThroughCheaperIntermediateHop, a pre-existing cost-routing test in a different file complaining about missing a fixture connector) that never recurred in the other runs - looks like pre-existing flakiness the design spec already flagged as a known risk category, not something this session's changes caused, but not fully confirmed either way.

Investigated and RULED OUT: hypothesized that `run/world`'s GameTest structure placement coordinates (all runs land somewhere in the single-digit-to-double-digit MILLIONS, e.g. X=-14902743, X=10034257) might be drifting further from origin across this session's many dozens of repeated `runGameTestServer` invocations, and that some subsystem hits float-precision issues at that scale. Deleted `run/world` (313MB, gitignored, pure disposable dev-server data - confirmed via `git check-ignore` before removing) to force a truly fresh world (entity IDs reset to 1, confirmed in the log) and re-ran 3 times: placements were STILL in the single-digit millions (X=-5508377, -8031150, -2078516) even on a brand-new world, and the same run-to-run inconsistency persisted. So large-coordinate placement is apparently normal/by-design for this GameTest setup, not accumulated drift - and IEEE754 double precision has ample headroom at 7-8 digit coordinates anyway (doubles are exact well past 10^15), so pure float-precision loss is not a well-supported theory either. Genuine root cause for the run-to-run inconsistency is still UNKNOWN as of this note.

STATUS: reverted all temporary diagnostics from both AbstractSiegeConstructionGoal.java and the test file (compiles clean). The lookahead-distance fix and the escape-hatch moveOrHop fix are both real, verified, low-risk improvements independent of this remaining problem, and worth keeping regardless of how issue 2 resolves. Task 2's own test does NOT reliably pass yet - stopping active live-debugging here per the human partner's own instruction to note this and move on to plan-level work; whoever resumes should treat this as a fresh, from-scratch root-cause investigation (the accumulated evidence so far: real progress happens on SOME runs (successful stair builds observed), genuine non-progress happens on others, with no coordinate-based or timing-based pattern identified yet that predicts which).

DECISION (human partner, explicit): clanrat siege construction/pathing is genuinely an unfinished system - that's the whole reason this test suite exists, to give the pathing work ahead of it something concrete to turn green against. Write the remaining 3 tests (Tasks 3-5) now, known-red, rather than blocking their existence on Task 2 passing first. If test quality is ever questioned later, the human partner will manually verify real in-game behavior against what the tests assert.

Tasks 3-5: added (commit 9c2a80a) - small group (4 rats), large group (10 rats), large group across a giant gap (dx=dy=44, forcing chained macro-project hops). Reused Task 2's helpers unchanged, including the carveGroundBeneathPlatform call the plan's own literal Task 3-5 code blocks predate (that fix was discovered during Task 2, after this plan document's Tasks 3-5 sections were written - applied here for consistency, matching Task 2's actual current implementation rather than the plan's now-stale snippets). Verified via a full suite run: all 4 tests in this file register, run to completion, and fail for the expected reason (rats never crossing the gap, not a crash/hang) - same 2 pre-existing unrelated failures as always, no new ones.

All 4 tasks in this plan are now scaffolded (Task 1 complete/passing infrastructure; Tasks 2-5 complete-as-scaffolding, known-red pending the pathing system itself being fixed). Per the human partner: next step is a fresh planning cycle (brainstorm -> spec -> plan) for the actual clanrat pathing/construction fixes, using this test suite as the verification target. This plan's own remaining work is done for now.
